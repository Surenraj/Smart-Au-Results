<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calcjit">

    <application
        android:allowBackup="true"
        android:icon="@drawable/see_gpa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyResultCse" />
        <activity android:name=".MyResult" />
        <activity android:name=".Cgpa2" />
        <activity android:name=".AnnaUnivWebView" />
        <activity android:name=".Cgpa" />
        <activity android:name=".Gpa2" />
        <activity android:name=".Gpa" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>